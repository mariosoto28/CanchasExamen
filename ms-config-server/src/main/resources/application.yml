server:
  port: 7000

spring:
  application:
    name: ms-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/mariosoto28/CanchasExamen.git
          searchPaths:
            - config-data
          default-label: main

  security:
    user:
      name: root
      password: 123456
